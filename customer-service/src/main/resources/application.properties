# Configuración de la aplicación
quarkus.application.name=customer-service
quarkus.application.version=1.0.0

# Configuración del servidor
quarkus.http.port=8080
quarkus.http.host=0.0.0.0


# ENVIRONMENT: inject DB config from ConfigMap and Secret
# ConfigMap provides: DATABASE_URL, DATABASE_USER
quarkus.openshift.env.configmaps=neuralbank-postgresql-config
# Secret provides: key 'password' mapped to env var DATABASE_PASSWORD
quarkus.openshift.env.vars.DATABASE_PASSWORD.value-from.secret=neuralbank-postgresql-secret
quarkus.openshift.env.vars.DATABASE_PASSWORD.value-from.secret-key=password

# Configuración de la base de datos PostgreSQL
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/neuralbank}
quarkus.datasource.username=${DATABASE_USER:neuralbank}
quarkus.datasource.password=${DATABASE_PASSWORD:neuralbank123!}


# Configuración de Hibernate
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.log.sql=true
quarkus.hibernate-orm.sql-load-script=no-file
quarkus.hibernate-orm.mapping.format.global=ignore

# Configuración de OpenAPI/Swagger
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger-ui
mp.openapi.extensions.smallrye.info.title=NeuralBank Customer Service API
mp.openapi.extensions.smallrye.info.version=1.0.0
mp.openapi.extensions.smallrye.info.description=Microservicio de gestión de clientes para NeuralBank
mp.openapi.extensions.smallrye.info.contact.email=dev@neuralbank.com
mp.openapi.extensions.smallrye.info.contact.name=NeuralBank Development Team

# Configuración de Health Check
quarkus.smallrye-health.enabled=true

# Configuración de logs
quarkus.log.console.enable=true
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n
quarkus.log.console.level=INFO
quarkus.log.category."com.neuralbank".level=DEBUG

# Configuración de CORS
quarkus.http.cors=true
quarkus.http.cors.origins=*
quarkus.http.cors.methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.exposed-headers=Content-Disposition
quarkus.http.cors.access-control-max-age=24H

# Configuración de JSON
quarkus.jackson.fail-on-unknown-properties=false
quarkus.jackson.write-dates-as-timestamps=false

# --- Configuración de Flyway ---
quarkus.flyway.migrate-at-start=true
quarkus.flyway.placeholders.create_extensions=CREATE EXTENSION IF NOT EXISTS "uuid-ossp"; CREATE EXTENSION IF NOT EXISTS "pgcrypto";
quarkus.flyway.placeholders.uuid_function=uuid_generate_v4()
quarkus.flyway.placeholders.generated_always_as=GENERATED ALWAYS AS
quarkus.flyway.placeholders.generated_stored=STORED
quarkus.flyway.placeholders.using_gin=USING GIN
quarkus.flyway.placeholders.partial_index_filter=WHERE dias_mora > 0
quarkus.flyway.placeholders.type_inet=INET
quarkus.flyway.placeholders.procedure_start=/*
quarkus.flyway.placeholders.procedure_end=*/


# Configuración de dev
%dev.quarkus.datasource.db-kind=h2
%dev.quarkus.datasource.jdbc.url=jdbc:h2:mem:neuralbankdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
%dev.quarkus.hibernate-orm.database.generation=none
%dev.quarkus.flyway.migrate-at-start=true
%dev.quarkus.h2.console.enabled=true
%dev.quarkus.h2.console.path=/h2
%dev.quarkus.flyway.placeholders.create_extensions=SELECT 1;
%dev.quarkus.flyway.placeholders.uuid_function=random_uuid()
%dev.quarkus.flyway.placeholders.generated_always_as=AS
%dev.quarkus.flyway.placeholders.generated_stored=/* stored */
%dev.quarkus.flyway.placeholders.using_gin=/* gin */
%dev.quarkus.flyway.placeholders.partial_index_filter=/* filter */
%dev.quarkus.flyway.placeholders.type_inet=VARCHAR(45)
%dev.quarkus.flyway.placeholders.procedure_start=/*
%dev.quarkus.flyway.placeholders.procedure_end=*/

# Configuración de test
%test.quarkus.datasource.db-kind=h2
%test.quarkus.datasource.jdbc.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
%test.quarkus.hibernate-orm.database.generation=none
%test.quarkus.flyway.migrate-at-start=false

# OPENSHIFT CONFIGURATION
quarkus.kubernetes.deployment-target=openshift
quarkus.openshift.route.expose=false 
quarkus.openshift.name=customer-service

# PORT
quarkus.openshift.ports.http.container-port=8080

# HEALTH CHECKS
quarkus.health.extensions.enabled=true
quarkus.smallrye-health.root-path=/q/health

# RESOURCES
quarkus.openshift.resources.requests.memory=256Mi
quarkus.openshift.resources.requests.cpu=250m
quarkus.openshift.resources.limits.memory=512Mi
quarkus.openshift.resources.limits.cpu=500m

